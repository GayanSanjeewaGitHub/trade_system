[project]
name = "trading-chatbot-frontend"
version = "1.0.0"
description = "Streamlit frontend for Trading Chatbot"
readme = "README.md"
requires-python = ">=3.10,<3.15"
dependencies = [
    "streamlit>=1.28.0",
    "requests>=2.31.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.uv]
dev-dependencies = []
